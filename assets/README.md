# Assets

Drop the Captain Americano poster here as `captain-americano-poster.jpg` before pushing to GitHub. The hero section of `index.html` references this path:

```
assets/captain-americano-poster.jpg
```

If you want to use a different filename or format, update the `<img>` src in `index.html`.
